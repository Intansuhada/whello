<div class="user-profile">
    <img src="{{ auth()->user()->avatar_url }}" 
         alt="Profile" 
         class="avatar"
         width="32" 
         height="32">
    <!-- ...existing code... -->
</div>
